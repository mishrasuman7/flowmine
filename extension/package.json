{
  "name": "flowmine-extension",
  "version": "0.1.0",
  "private": true,
  "description": "FlowMine Chrome extension — observes browser activity and executes generated skills.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "0.0.270",
    "esbuild": "0.25.0",
    "typescript": "5.9.3"
  }
}
