/**
 * FlowMine background service worker — placeholder.
 *
 * Replaced in the next commit with the real event capture + flush
 * implementation. This stub exists so the esbuild scaffold compiles and the
 * extension folder is loadable as soon as the manifest is in place.
 */

export {};

console.log('[flowmine] background worker loaded (stub)');
