/**
 * FlowMine — seed-events.ts (placeholder).
 *
 * Real generator lands in the next commit. This file exists so the scripts
 * scaffold typechecks and `pnpm seed:dry` is recognised as a valid script.
 */

console.log('[flowmine] seed-events placeholder — implementation in next commit');
